{
  "name": "scraw-api",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npm run clean && tsc -p tsconfig.json",
    "clean": "rimraf dist",
    "start": "npm run build && node dist/index.js start",
    "start:generate": "npm run build && node dist/index.js generate",
    "start:static": "node dist/index.js",
    "check": "eslint src --ext .ts",
    "fix": "eslint src --ext .ts --fix"
  },
  "bin": {
    "scraw-api-update": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^11.12.2",
    "@typescript-eslint/parser": "^1.5.0",
    "commander": "^2.19.0",
    "discord.js": "^11.4.2",
    "eslint": "^5.16.0",
    "gts": "^0.9.0",
    "overwatch-api": "^0.7.5",
    "rimraf": "^2.6.3",
    "typescript": "~3.3.3333"
  }
}
